version: '3.8'

services:
  galactic_dev:
    image: galactic_dev_image
    container_name: galactic_dev_container
    volumes:
      - ./src:/root/ros_ws/src:delegated
    tty: true
    stdin_open: true
